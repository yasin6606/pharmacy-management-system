/** @type {import('jest').Config} */
module.exports = {
  preset: 'ts-jest',
  testEnvironment: 'node',
  roots: ['<rootDir>/tests'],
  testMatch: ['**/*.test.ts'],
  clearMocks: true,
  // Keep tests independent of real DB / Redis
  setupFiles: ['<rootDir>/tests/setup.ts'],
};
